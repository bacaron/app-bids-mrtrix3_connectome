#!/bin/bash
#PBS -l nodes=1:ppn=8,walltime=24:00:00
#PBS -N app-bids-mrtrix3_connectome

for prm in $(jq -r 'keys[]' config.json | grep -Ev ^_); do
    val=$(jq -r ".$prm" config.json)
    eval "$prm=\"$val\""
done

if [[ $preprocessed == true ]]; then
    pp_arg="--preprocessed"
fi
if [[ -n $streamlines && $streamlines != null ]]; then
    sl_arg="--streamlines $streamlines"
fi

#clean up previous job (just in case)
rm -rf ${bids_sub} output input

#BIDS
function check_required_fields {
    #these fields are required for preprocessed=true
    local json=$1
    local f1=$(jq '.TotalReadoutTime' $json)
    local f2=$(jq '.PhaseEncodingDirection' $json)
    [[ -n $f1 && -n $f2 && $f1 != null && $f2 != null ]]
}

function copy_to_bids {
    # inputs:   (1) bids subdirectory, (2) bids image suffix,
    #           (3) BL variables name, (4) optinal run # suffix

    local sdir=$1
    local itype=$2
    local vname=$3
    local suffix=$4
    local msg

    if [[ -n $suffix ]]; then
        suffix=${suffix}_
    fi

    local bids_dir=input/${bids_sub}/$sdir
    local bids_name=$suffix$itype

    mkdir -p $bids_dir
    #let's cp because ln is problematic when relative path is used
    cp ${!vname} ${bids_dir}/${bids_sub}_${bids_name}.nii.gz
    #make BIDS json sidecar from BL metadata
    jq --arg idprm $vname '._inputs[] | select(.id == $idprm) | .meta' \
        config.json > ${bids_dir}/${bids_sub}_${bids_name}.json
    if [[ $sdir == dwi ]]; then
        if [[ $preprocessed == false ]] && \
            ! check_required_fields ${bids_dir}/${bids_sub}_${bids_name}.json; then
            msg="TotalReadoutTime and PhaseEncodingDirection BIDS fields required "
            msg+="to run mrtrix3_connectome preprocessing. Missing for ${!vname}"
            echo $msg
            exit 1
        fi
        local vbval=${vname/dwi/bvals} #BL names with "s"
        local vbvec=${vname/dwi/bvecs}
        cp ${!vbval} ${bids_dir}/${bids_sub}_${bids_name}.bval
        cp ${!vbvec} ${bids_dir}/${bids_sub}_${bids_name}.bvec
    fi
}

sub=$(jq -r "._inputs[0].meta.subject" config.json | tr -d "_")
if [[ -z $sub || $sub == null ]]; then
    sub=0
fi
bids_sub=sub-$sub

copy_to_bids anat T1w t1

if [[ -n $dwi2 && $dwi2 != null ]]; then
    copy_to_bids dwi dwi dwi2 run-02
    run_suffix=run-01
elif [[ $preprocessed == false ]]; then
    echo "Two DWI images are required to run mrtrix3_connectome preprocessing."
    exit 1
fi

copy_to_bids dwi dwi dwi $run_suffix

singularity run -e \
    docker://katealpert/bids-mrtrix3_connectome \
    input . participant \
    -n 8 --parcellation $parcellation $sl_arg $pp_arg \
    --participant_label $sub
res=$?

if [[ -d ${bids_sub} ]]; then
    mv ${bids_sub} output
    rm -rf input
fi

#exit code from the last command (singularity) will be used.
exit $res
